package com.diploma.repository;

import com.diploma.entities.PilotDirectory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PilotDirectoryRepository extends JpaRepository<PilotDirectory, Integer> {
}
